

const Header = ({title}) => {
  return (
    <header>
      <h1>{title}</h1>
    </header>
  );
}

// Header.defaultProps = {
//     title: 'Task-Tracker',
// }
// Adding CSS style
// const styles = {
//   color: "red",
//   backgroundColor: "black",
// };

export default Header
